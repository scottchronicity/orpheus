# GPS Service Dependencies
# NOTE: This project uses requirements.txt for dependencies
# for compatibility with older pip versions (Python 3.9.5).

# Core dependency - installed from local path in production
-e ../../platform/orpheus-common

# MQTT client
paho-mqtt>=2.1.0

# YAML configuration
PyYAML>=6.0,<7.0

# GPS serial communication
pyserial>=3.5,<4.0

# NMEA sentence parsing
pynmea2>=1.18.0,<2.0

# Development/Testing dependencies
ruff>=0.8.0
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
