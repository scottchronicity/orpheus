# Hardware Abstraction Layer

Hardware abstraction libraries for audio and video interfaces.

## Overview

Platform-agnostic hardware interface libraries that can work across different boards and configurations.

## Structure

- `audio/` - Audio capture and playback interfaces
- `video/` - Video capture and processing interfaces

## Design Philosophy

These libraries abstract away platform-specific details, allowing agents to work with hardware through a consistent API.
