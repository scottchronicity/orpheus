[pytest]
testpaths = .
python_files = test_*.py
addopts = -v --strict-markers
markers =
    real_audio: full real-audio cascade against the running docker fleet (gated by ORPHEUS_E2E_REAL_AUDIO + a reachable broker; never runs in CI)
