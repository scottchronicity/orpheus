[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers
pythonpath = src
asyncio_mode = auto
markers =
    e2e: behavior-focused end-to-end test requiring a live broker (skips if absent); excluded from the unit gate
