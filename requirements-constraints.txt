# Orpheus Monorepo - Shared Dependency Versions
# ==============================================
# This file documents the version constraints used across all projects.
# Each project's requirements.txt should mirror these versions.
#
# Python Version: 3.9.5+ (Jetson Orin NX compatibility)
# Last Updated: December 2024
#
# To check for consistency, run: make check-deps
#
# When updating a version:
#   1. Update this file
#   2. Update the same version in all requirements.txt files that use it
#   3. Run tests in each affected project

# Core Data & Config
pydantic>=2.10.0,<3.0.0
pyyaml>=6.0.2

# Networking & API
requests>=2.32.0
httpx>=0.28.0
nats-py>=2.6,<3
paho-mqtt>=2.1.0,<3.0.0

# Web Framework (dashboard)
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.12

# System & Hardware
psutil>=7.1.0
Pillow>=11.0.0

# Audio Processing (audio-motion agent)
numpy>=1.26.0,<3.0
soundfile>=0.12.0
sounddevice>=0.5.0

# Development & Testing (all projects)
# ruff is pinned EXACTLY, not floored: new ruff releases stabilize new lint
# rules, so a floating version makes CI lint fail on unchanged code the day a
# release lands (ruff 0.16.4 / PLR0917, 2026-08). Upgrade deliberately: bump
# the pin here + every component's pyproject dev extras + the UI backend
# requirements.txt together, run `make lint`, and fix what the new version
# flags in the same change.
ruff==0.15.20
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
debugpy>=1.8.0
