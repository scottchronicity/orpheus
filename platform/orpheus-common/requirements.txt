# Orpheus Common Library - Dependencies
# Python 3.9+ compatible
#
# Version constraints are aligned with requirements-constraints.txt in repo root.
# When updating versions, update both files to maintain consistency.

# Core dependencies
pydantic>=2.10.0,<3.0.0
pyyaml>=6.0.2
nats-py>=2.6,<3
paho-mqtt>=2.1.0,<3.0.0
requests>=2.32.0
psutil>=7.1.0
Pillow>=11.0.0
structlog>=23.2.0  # Structured logging with color support
# systemd-python>=235  # Optional: Install on Jetson for JournalHandler support

# Development/Testing dependencies
ruff==0.15.20
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
debugpy>=1.8.0
